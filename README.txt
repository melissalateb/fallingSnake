il faut d'abord entrer dans le package pour avoir acces au fichiers .java avec la commande :
cd fallingSnake
ensuite il faut compiler le projet avec la ligne de commande suivante:
javac *.java
il faudras ensuite sortir du package:
cd..
ensuite on execute le projet avec la ligne de commande suivante:
java fallingSnake.Main